# Railix II Progress

Status date: 2026-07-18

Progress percentages in this file are evidence-based estimates from working code, green build results, and test coverage already present in the repository. They are not wishful thinking.

## Current focus

- Current roadmap item: `Slice 4: DataWorkbench`
- Current sub-item: `Contract-Aware Connection Authoring`
- Status: `Planned`
- Estimated complete: `0%`
- Estimated left to integrate: `100%`

The next milestone will project `Step Contract 1.0` ports, shapes, configuration defaults, and semantics into the Creator; permit only structurally compatible connections; edit ordered connection operators; preview nested primitive trees; persist and reload JSON/YAML plans without semantic drift; and prove every supported/rejected authoring path through the real browser or Creator HTTP boundary.

## Step Contract 1.0 checkpoint

- `Step Contract 1.0 and Connection Semantics` is `Done` at `100%` complete and `0%` left to integrate.
- Bytewright and Gatekeeper completed bounded independent reviews with no actionable contract defects or missing reachable Step Contract/connection scenarios.
- A headed Playwright run against the real named-module Creator completed with `0` browser console errors and `0` warnings.
- Completion screenshot: `output/playwright/railix-step-contract-1.0-100.png`.

## Packaging checkpoint

- `Slice 5: Packaging` is now `Done` at `100%` complete and `0%` left to integrate.
- Headless packaged launchers remain proven, and the Creator UI/static-assets surface is now also proven on the current host through the public `railix package --mode creator` app-image boundary.
- Packaged Creator launch is still explicit rather than magical: the current proven launcher starts with `--repo-root` and optional `--runs-root`, not Finder-style one-click workspace discovery.
- Production-readiness and no-leak claims remain blocked by missing long-lived packaged-runtime soak or leak instrumentation evidence.

## Current evidence

- `mvn -q -pl modules/railix-kernel -am verify` passes with `425` kernel tests, `96.59%` line coverage (`3941/4080`), and `90.76%` branch coverage (`1502/1655`), above the enforced `95%` line and `90%` branch gates.
- `mvn -q verify` passes across the full reactor, including the real launcher and packaging integration paths.
- `mvn -q -pl modules/railix-kernel -am -DskipTests javadoc:javadoc` passes for the public kernel API.
- `railix-creator` now has `82` green scenarios across `3` test files, including the dedicated embedded-favicon HTTP regression used by the clean-console screenshot gate.
- `Step Contract 1.0` now has structural `Shape` contracts, typed named input/output ports, typed configuration with explicit-null defaults, machine-readable execution semantics, stable codec round-trips, resolver identity checks, and one frozen immutable contract snapshot per built Step invocation.
- `AppPlan.Connection` now owns explicit source/target ports and an ordered pure operator pipeline. The kernel validates topology and structural assignability before execution, binds named values deterministically, materializes configuration defaults, and validates every declared runtime output.
- JSON and YAML authoring/normalization paths are covered in the current kernel boundary. XML normalization is not implemented and is tracked explicitly by `Slice 15`; no XML support is claimed by this milestone.

- `mvn -q -pl modules/railix-kernel,modules/railix-std-data,modules/railix-std-store -am -Dtest=StepTest,KernelContractCodecTest,StandardDataStepProviderTest,StandardStoreStepProviderTest -Dsurefire.failIfNoSpecifiedTests=false test` passes after landing step-local config plumbing, legacy `ExecutionInput` overload compatibility, and the first real `DataTransform` slice.
- `mvn -q -pl modules/railix-kernel,modules/railix-std-data -am -Dtest=InMemoryContextDocTest,StandardDataStepProviderTest,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataTransformPack,StandardDataPackIntegrationTest#shouldRunLauncherThroughNamedModulePathWithInstalledDataTransformPack -Dsurefire.failIfNoSpecifiedTests=false test` passes after landing selector-path SPI support in `ContextDoc`, concrete selector-path exposure from `InMemoryContextDoc`, repeat target materialization, alias-scoped repeat expression resolution, and installed-pack launcher proof for the repeated-order transform shape.
- `mvn -q -pl modules/railix-std-data -am -Dtest=StandardDataStepProviderTest,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataValidatePackOnValidInput,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataValidatePackOnInvalidInput,StandardDataPackIntegrationTest#shouldRunLauncherThroughNamedModulePathWithInstalledDataValidatePackOnInvalidInput -Dsurefire.failIfNoSpecifiedTests=false test` passes after landing the first real `DataValidate` slice: provider-owned required-path validation plus valid/invalid branch proof through in-process and named-module installed-pack execution.
- `mvn -q -pl modules/railix-std-data -am -Dtest=StandardDataStepProviderTest,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataRoutePackOnApprovalBranch,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataRoutePackOnAutoBranch,StandardDataPackIntegrationTest#shouldRunLauncherThroughNamedModulePathWithInstalledDataRoutePackOnApprovalBranch -Dsurefire.failIfNoSpecifiedTests=false test` passes after landing the first real `DataRoute` slice: ordered route evaluation, explicit no-match rejection, approval/auto branch proof, and named-module installed-pack execution.
- `mvn -q -pl modules/railix-std-data -am -Dtest=StandardDataStepProviderTest#shouldResolveDataForEachStepWithCursorStateAndLoopOutcomes,StandardDataStepProviderTest#shouldReturnEmptyForEachOutcomeAndRejectMalformedForEachConfig,StandardDataStepProviderTest#shouldSupportCustomForEachStatePath,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataForEachPackOnPopulatedSelector,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataForEachPackOnEmptySelector,StandardDataPackIntegrationTest#shouldRunLauncherThroughNamedModulePathWithInstalledDataForEachPackOnPopulatedSelector,StandardDataPackIntegrationTest#shouldBuildMacPkgInstallerArtifactFromBuilderEmittedPackagedSettings -Dsurefire.failIfNoSpecifiedTests=false test` passes after landing the first real `DataForEach` slice: plan-loop cursor preparation, guarded cursor-state ownership, reserved cursor-alias rejection, custom `statePath` proof, named-module installed-pack execution, and the packaging-script runtime-image isolation fix that removes the shared `build/image-dev` collision from custom app-image builds.
- `mvn -q -pl modules/railix-std-data -am -Dtest=StandardDataStepProviderTest#shouldResolveDataAggregateStepWithCountSumMinMaxCollectOutputs,StandardDataStepProviderTest#shouldReturnEmptyForAggregateSelectorMissAndRejectMalformedAggregateConfig,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataAggregatePackOnPopulatedSelector,StandardDataPackIntegrationTest#shouldRunKernelFlowThroughInstalledDataAggregatePackOnEmptySelector,StandardDataPackIntegrationTest#shouldRunLauncherThroughNamedModulePathWithInstalledDataAggregatePackOnPopulatedSelector -Dsurefire.failIfNoSpecifiedTests=false test` passes after landing the first real `DataAggregate` slice: selector-driven `count|sum|min|max|collect` aggregation into `ctx`, numeric-string coercion for numeric ops, explicit empty-branch behavior, and named-module installed-pack execution.
- `mvn -q -pl modules/railix-std-data -am -Dtest=StandardDataStepProviderTest,StandardDataPackIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test` now passes with the full provider-plus-packaging class suite after the same `DataForEach` and packaging-script hardening, including the previously failing macOS builder-emitted packaged-settings path.
- `mvn -q -pl modules/railix-std-data -am -Dtest=StandardDataStepProviderTest,StandardDataPackIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test` now also passes after landing `DataAggregate`, keeping the full provider-plus-packaging class suite green while adding the new aggregate step and launcher proof.
- `mvn -q -pl modules/railix-std-data -am -Dtest=StandardDataPackIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test` now passes with `49` green packaging-heavy integration scenarios after adding the `DataAggregate` in-process and named-module execution coverage on top of the earlier process-output and runtime-image isolation fixes.
- `mvn -q -pl modules/railix-std-data -am test` now passes with `66` green tests for the full module (`49` integration + `17` provider), so the current `DataAggregate` slice is verified in the same module run that exercises the packaging-heavy launcher paths.
- `mvn -q -pl modules/railix-std-data -am -Dtest=StandardDataPackIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test` also passes in isolation, proving the full packaging-heavy `railix-std-data` integration suite completes cleanly once the contested concurrent verifier state is removed.
- `mvn -q -pl modules/railix-kernel,modules/railix-creator -am -Dtest=StepProviderCatalogTest,PackBackedStepResolverTest,CreatorRemoteExecutionBoundaryTest,CreatorShellMainTest,CreatorRunLauncherTest -Dsurefire.failIfNoSpecifiedTests=false test` now passes after extending the shared instance contract with the ninth honest `Slice 10` seam: a dedicated `POST /api/remote-execution/preflight` boundary now evaluates the same request contract without executing it, admits same-instance work with explicit no-side-effect semantics, carries discovered-peer blocker diagnostics through a public cross-instance admission surface instead of burying them only inside the execution request path, rejects any non-zero `timeoutMillis` value consistently on both the request and preflight paths, and now proves the full discovered-peer preflight blocker matrix at the public shell boundary for inactive peers, incomplete target identity, target digest mismatch, target unsupported step use, target unsupported resource refs, and capability-aligned-but-undeliverable cross-instance requests. The Creator suite now stands at `81` green Creator scenarios (`79` shell/HTTP cases, `1` direct remote-boundary drift regression, and `1` launcher-retention seam proof). The same public boundary still proves the eight `Slice 9` foundation seams, and it now proves nine real `Slice 10` truth surfaces without overclaiming cross-instance execution: each live instance publishes deterministic runtime provider identity derived from the installed `StepProvider` catalog (`providerModuleIds`, `supportedUses`, resolved non-settings resource-ref patterns, and a capability digest when the use catalog is complete); each live instance advertises a concrete `POST /api/remote-execution/requests` request boundary with deterministic accepted/rejected outcomes; that boundary now accepts same-instance inline execution not only for no-resource/no-runtime-permission payloads but also for resource-backed and settings/permission-bound local steps when the request carries caller-supplied granted permissions plus a target-local `settingsLocation`; that same boundary also accepts `replyMode=DEFERRED` for same-instance execution with a persisted `queued -> executed|failed|interrupted` lifecycle, stable `executionId`, and explicit bounded overload rejection when the local deferred queue is unavailable; unsupported `timeoutMillis` is now rejected explicitly for every non-zero value instead of being silently ignored; resolver/catalog drift is now rejected deterministically as `runtime-capability-drift` instead of failing later as a request-path `500`; cross-instance targeting now distinguishes a truly unknown target from a discovered peer and returns target-side capability diagnostics (`discoveredTarget*`) plus explicit rejection codes for inactive peers, incomplete target runtime identity, target digest mismatch, target use/resource mismatch, or capability-aligned-but-undeliverable cross-instance requests; a dedicated `POST /api/remote-execution/preflight` admission contract now exposes the same honesty surface without starting execution or writing request-history artifacts and proves those blocker branches through the public shell boundary; and the Creator shell still exposes a bounded artifact-backed remote request history surface with queued/executed/failed/interrupted/rejected counts plus request-to-run linkage. The current Creator runtime still keeps that identity surface narrower than pack identity: it is explicitly provider-backed only, explicitly not workspace pack install state, explicitly not remote execution compatibility, and it leaves `runtimePackIdentitySupported=false` in the distributed-support matrix. The accepted boundary remains narrow and explicit: it is still same-instance-only, still `acceptedExecutionSupported=true`, now `acceptedExecutionScope=same-instance-inline-or-deferred`, now advertises `supportedReplyModes=["NONE","IMMEDIATE","DEFERRED"]`, now advertises `queueSupported=true`, now advertises `preflightSupported=true`, and it still does not claim cross-instance execution, distributed queueing, shared cache digests, or remote permission propagation. The lifecycle/report surface remains equally explicit and bounded: `GET /api/remote-execution/requests?limit=<n>` returns recent artifact-backed request rows with accepted/rejected/unsupported counts, queued/executed/failed/interrupted counts, request ids, execution ids, step uses, boundary decisions, HTTP response codes, request durations, and linked run ids when the request executed. The shell headline and section copy still say `Slice 10 / Remote Execution Boundary`, and the UI still renders a `Recent Remote Requests` panel so this seam is visible instead of hidden behind raw files. The current Creator runtime still keeps its execution payload small and honest: local step execution supported, same-instance inline/deferred remote-request execution supported, cross-instance remote step execution unsupported, cross-instance discovered-peer preflight/admission diagnostics supported, queued step execution supported only as a same-instance bounded local queue, cache sharing unsupported, and cross-instance permission sharing unsupported. The distributed-support payload stays equally explicit: status `foundation-only`, runtime pack identity unsupported, remote execution boundary supported, distributed queue unsupported, cross-instance permission propagation unsupported, and shared cache digest unsupported. The alignment surface is still intentionally not sold as runtime identity: it reports repo metadata and runtime metadata side by side, with exact matched, workspace-only, and runtime-only ids. In the seeded Creator boundary, that currently means `1` workspace pack (`railix.demo.pack`), `4` runtime provider modules, `0` matched ids, `1` workspace-only id, and `4` runtime-only ids. The current Creator runtime still keeps its actual runtime capability payload smaller and honest: `4` installed provider modules, `15` supported uses, `1` supported resource-ref pattern (`store/*`), `0` unreported providers, trust mode `loopback-only`, shared-registry discovery enabled, remote control unsupported, cross-instance remote execution unsupported, provider-backed runtime identity digest publication when the use catalog is complete, same-instance inline/deferred remote request boundary advertisement, dedicated remote execution preflight advertisement, discovered-peer blocker diagnostics, live Creator load (`activeControlSessions` / `maxActiveSessions` and bounded deferred remote-request load), and raw host runtime identity (`osName`, `osArch`, `javaVersion`). The kernel catalog still rejects two capability-drift cases explicitly instead of flattening them into partial truth: duplicate advertised use ownership across providers and providers that advertise a use but do not resolve it. The suite still explicitly does not mark workspace pack metadata as installed runtime capability, does not mark provider-backed identity as runtime pack identity, does not mark cross-instance preflight/admission diagnostics as cross-instance remote execution support, and does not pretend a distributed scheduler, permission propagator, or remote runner already exists.
- `mvn -q -pl modules/railix-kernel -am -Dtest=BuiltRailixAppLoaderTest,BuiltRailixAppMainTest -Dsurefire.failIfNoSpecifiedTests=false test` now passes with `40` green launcher/loader scenarios (`16` loader plus `24` launcher) after landing the first real `railix.app.yaml` and envelope YAML runtime bridge at the existing launcher boundary: `BuiltRailixAppLoader.loadPlan(...)` and `loadEnvelope(...)` now accept a narrow supported YAML subset beside JSON, normalize lowercase reply-channel modes, reject unsupported multi-flow authoring specs explicitly, and keep incomplete or malformed examples as hard failures instead of fake success.
- A fresh `mvn -q -pl modules/railix-creator -am test` run again refreshed green surefire XML across `creator`, `kernel`, `std-data`, and `std-http` classes after the same slice, although the local Maven session still did not emit a clean final shell exit in this environment before the current progress update. The refreshed reports still keep the Creator shell, launcher, and dependent runtime classes green together rather than isolating the new history behavior in a single handpicked class run.
- The latest full-reactor `mvn verify -DtrimStackTrace=false` evidence in the repo still passed cleanly after the `DataValidate` slice; the current turn revalidated `railix-std-data` separately, and that module now completes `66` tests including the ~150-second packaging-heavy `StandardDataPackIntegrationTest`.
- `mvn -q -pl modules/railix-kernel,modules/railix-std-file -am -Dtest=LocalExecutionKernelTest,RuntimeRecordsTest,StandardFileStepProviderTest,StandardFilePackIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test` passes after the current-run std-file provenance-registry slice.
- `mvn -q -pl modules/railix-kernel verify` passes after landing the first honest pack-backed execution boundary.
- `railix-kernel` now includes `Shape`, selector wildcard matching, canonical `RailixPath` rendering with escaping support, and non-null `Reply.none()` refs.
- `PermissionSet` now models `requested`, `granted`, and per-resource `decisions`.
- `RunSignal` now exposes the shared envelope fields plus `step.failed`, `setting.read`, and `resource.created` signal types.
- `StepContract` and `SettingsTree` preserve more of the spec shape directly, including named ports, metric lists, settings description, and explicit encrypted/reference setting values.
- `Patch` now models explicit literal versus expression operands for `set`, `append`, and `merge`.
- `RailixPath` now rejects malformed separators, blank indexes, rootless index paths, and direct constructor use with an index-first root.
- `SettingsTree` now has kernel-owned behavior for sticky secret inheritance, cache-key-safe projection, runtime read auditing, and UI-safe projections.
- `KernelContractCodec` now provides a deterministic primitive UI model plus stable JSON/YAML rendering for `RailixValue`, `Selector`, `Patch`, `Shape`, `SettingsTree`, `PermissionSet`, `StepContract`, `OperatorContract`, `Envelope`, `Reply`, and `RunSignal`.
- Stable YAML rendering now preserves empty containers as real `[]` and `{}` values instead of collapsing them into implicit `null`.
- Secret `SettingsTree` values that are already materialized now fail explicitly at the UI-model codec boundary instead of being exported as plain text.
- Object-valued codec boundaries now fail with explicit field-level `IllegalArgumentException` messages instead of leaking raw `ClassCastException` when callers feed non-object values into `Envelope`, `Reply`, or similar object-only fields.
- `Shape.RefShape` metadata decoding now fails with an explicit field-level `IllegalArgumentException` instead of a raw cast failure when malformed UI models supply a non-object metadata shape.
- Stable JSON/YAML rendering now escapes control characters correctly and quotes YAML keys deterministically, so arbitrary contract strings and map keys no longer produce invalid or ambiguous output.
- Integer-valued decode boundaries now reject out-of-range `Long` inputs explicitly instead of silently truncating public contract fields.
- Slice 1 now has contract-level round-trip proof for the kernel UI model boundary, and secret materialization is canonically modeled as `setting.read` rather than a second synthetic audit link.
- `KernelContractCodec` now round-trips every public `RunSignal` subtype and a composite `RailixValue` carrying all current reference kinds through the UI-model boundary.
- `RailixPath`, `Shape`, `SettingsTree`, and `RunSignal` tests now pin more constructor, accessor, and edge-branch behavior directly through their public contracts.
- Runtime record coverage now includes `ContextDoc.ContextDiff`, `Step.ExecutionInput`, and `Step.Result` through public copy/accessor behavior instead of leaving those records dark.
- `Slice 2` now has a real public kernel boundary through `LocalExecutionKernel.run(...)`, backed by `AppPlan`, `StepResolver`, `InMemoryContextDoc`, and `RunSignalLog`.
- The local execution kernel now supports deterministic run folders, sequential step dispatch, wildcard step routing, context patch application, context-derived reply fallback, reply-channel validation, and append-only NDJSON signal logs.
- Runtime failure handling now degrades resolver failures, step failures, undeclared outcomes, and invalid final reply state into failed run records with terminal run artifacts instead of raw late crashes.
- Step execution now receives a kernel-owned settings facade instead of raw settings state, so settings reads stay deterministic at the `LocalExecutionKernel.run(...)` boundary.
- Settings-backed runs now emit `permission.decided` and `setting.read` signals from the kernel path, including granted, denied, missing, and encrypted-setting execution cases.
- The local execution kernel now honors `StepContract.RetryPolicy.maxAttempts` for retryable step-execution failures, carries real 1-based attempt numbers through `step.*`, `permission.decided`, and `setting.read`, and keeps failed attempts from mutating the committed context.
- Deterministic settings-boundary failures, undeclared outcomes, invalid patch application, and final reply failures now fail explicitly without fake retries, while retry backoff uses the contract duration directly in the local runner path.
- The local execution kernel now emits kernel-owned retry metrics through `metric.emitted`, exposing terminal `step.attempt` and `step.retryCount` values for both retry-success and retry-exhausted runs at the public `LocalExecutionKernel.run(...)` boundary.
- `Step.ExecutionInput` now exposes a kernel-owned resource scope, and the local execution kernel now emits `resource.created` plus deterministic `AutoCloseable` cleanup across successful runs, late terminal reply failures, and retry attempts without double-close at the public runtime boundary.
- `RunSignalLog` no longer retains emitted `RunSignal` objects in memory for the life of the returned run record: persisted signals are now rehydrated from `signals.ndjson` on demand, `signalCount()` is counted from the artifact itself, and malformed artifact cases now fail with explicit parser diagnostics.
- `RunSignalLog` now exposes bounded and streaming read surfaces through `readSignals(offset, limit)`, `tailSignals(limit)`, and `forEachSignal(...)`, while the legacy `signals()` replay path remains only as a deprecated compatibility shim.
- Cleanup-failure branches at the public `LocalExecutionKernel.run(...)` boundary are now covered: attempt cleanup failure now surfaces the cleanup error through `step.failed`, terminal run-scope cleanup failure is proven to degrade the run to `failed` without double-close, and multi-resource throwing cleanup is proven to run in reverse order without stopping at the first close failure.
- Terminal cleanup artifact ordering is now harmonized: `reply.produced` is emitted only after successful run-scope cleanup, so failed terminal cleanup no longer leaves a contradictory reply artifact behind a failed run record and failed summary.
- Terminal post-step failures now persist dedicated summary diagnostics: failed `summary.json` artifacts now carry an optional `terminalFailure` object with `phase`, exception `type`, `message`, and nested `suppressed` failure details, so reply-validation and run-cleanup degradations no longer disappear into a generic failed outcome.
- Built-app bootstrap now exists outside tests: `BuiltRailixApp` owns an embedded `AppPlan`, instantiates pack-backed execution through main-source collaborators, and provides a concrete runtime surface that executes flows without test-only assembly of `LocalExecutionKernel` plus `PackBackedStepResolver`.
- App/flow-level permission grant resolution now exists at the kernel boundary: `AppPlan` carries plan-level grants, runtime permission decisions merge declared step requests with plan grants, and granted settings reads can now be authorized by the embedded app plan rather than only by per-step wiring.
- Pack-backed execution now exists at the kernel boundary: `railix.kernel` exposes a `StepProvider` SPI plus `PackBackedStepResolver`, loads installed providers through `ServiceLoader`, rejects duplicate `use` ownership explicitly, and degrades missing step uses into deterministic `Unknown step use: ...` failures instead of null-shaped resolver noise.
- `railix-std-data` now contains the first real standard-pack step provider through `std.data.capture-payload`, and the reactor has a green integration test that drives the new `BuiltRailixApp` bootstrap surface through that installed pack path end to end.
- `railix-std-trigger` now contains the first real trigger-pack provider through `railix.std.trigger.ManualTrigger` and `railix.std.trigger.CliTrigger`, each normalizing the public `Envelope` boundary into trigger-owned context fields instead of leaving `railix-std-trigger` as a marker-only module.
- `railix-std-trigger` now has green installed-pack proof on two public boundaries: in-process `BuiltRailixApp` execution and named-module `BuiltRailixAppMain` launch through `ServiceLoader`.
- `railix-std-store` now contains the first real store-pack provider through `railix.std.store.StoreRead`, `StoreWrite`, `StoreDelete`, `StoreExists`, and `StoreList`, backed by a filesystem store root at `settings.store.rootDir` instead of marker manifests only.
- `railix-std-store` now accepts explicit `null` payload values as real stored data, distinguishes missing versus present-null through step outcome, and replaces `entry.json` via temp-file move semantics instead of in-place overwrite.
- `railix-std-store` now has green installed-pack proof on three public boundaries: in-process `BuiltRailixApp` execution, named-module `BuiltRailixAppMain` launch through `ServiceLoader`, and the scripted `jlink` packaged launcher path.
- `railix-kernel` now exposes a kernel-owned metric emission boundary on `Step.ExecutionInput.metrics()`, and the local execution kernel persists step-owned `metric.emitted` signals only for contract-declared descriptors with contract-declared label keys.
- `railix-std-metrics` now contains the first real metrics-pack provider through `railix.std.metrics.MetricEmit`, which emits the first narrow standard descriptor `railix.metric.value` instead of pretending arbitrary runtime metric names or units are already safe.
- `railix-std-metrics` now has green installed-pack proof on two public boundaries: in-process `BuiltRailixApp` execution and named-module `BuiltRailixAppMain` launch through `ServiceLoader`.
- `railix-kernel` now exposes a kernel-owned audit emission boundary on `Step.ExecutionInput.audits()`, and the local execution kernel persists step-owned `audit.emitted` signals immediately without buffering them in the returned run record.
- `railix-std-audit` now contains the first real audit-pack provider through `railix.std.audit.AuditCustom`, which emits a narrow custom audit record with a non-blank event name plus object-only data instead of pretending a broader audit contract already exists.
- `railix-std-audit` now has green installed-pack proof on two public boundaries: in-process `BuiltRailixApp` execution and named-module `BuiltRailixAppMain` launch through `ServiceLoader`.
- `railix-std-file` now contains the first real file-pack provider through `railix.std.file.FileWrite`, `FileRead`, and `FileDelete`, limited intentionally to stable-JSON file operations under `settings.file.rootDir` instead of pretending arbitrary host-filesystem access is already safe.
- `railix-std-file` now emits real `FileRef` values plus `resource.created` metadata on write, reads by path or existing file ref, deletes by path or existing file ref, rejects traversal/out-of-root ref misuse, uses temp-file move semantics for writes, and resolves new relative refs only through current-run kernel provenance instead of trusting caller-supplied path strings.
- `railix-std-file` now has green installed-pack proof on two public boundaries: in-process `BuiltRailixApp` execution and named-module `BuiltRailixAppMain` launch through `ServiceLoader`.
- `railix-std-http` now contains the first real protocol-pack provider through `railix.std.http.HttpTrigger`, plus a real HTTP request-envelope mapper, immediate-reply adapter, and synchronous JDK `HttpServer` boundary in `EmbeddedHttpBoundary` instead of marker metadata only.
- `railix-std-http` now has green protocol-boundary proof on two public surfaces: focused module tests through a real `HttpServer` plus `HttpClient`, and a full green reactor verify that includes installed-pack `ServiceLoader` execution.
- `railix-std-http` now also persists one stable per-run `http-capture.json` artifact for handled requests, containing protocol, method, path, repeated query values, request headers, request payload, response status, response headers, reply mode, run outcome, and boundary duration instead of leaving HTTP request evidence transient only in memory.
- `railix-std-http` now also exposes `HttpTrafficPanelQuery`, a public local-run query surface that streams traffic rows from persisted `http-capture.json` artifacts without inventing a Creator panel runtime that still does not exist in the repo.
- `railix-std-http` now also snapshots request data before envelope mapping, persists boundary-owned rejection captures for malformed or unsupported pre-run HTTP requests, and surfaces those rejections through the same local traffic read path instead of letting mapper-level failures disappear from local evidence.
- `railix-std-http` now also exposes `HttpTrafficReport`, a stable-JSON report surface over the local HTTP capture artifacts, including handled/rejected/boundary-failure counts and status distribution for downstream UI or CLI consumption.
- `railix-std-http` now also exposes `HttpTrafficReportMain`, a minimal public report entrypoint that reads persisted local HTTP capture artifacts from `--runs-root` and emits stable JSON instead of requiring downstream callers to embed Java against the report class directly.
- `railix-std-http` now also persists boundary-owned failure captures for unexpected post-acceptance runtime faults and surfaces them through the same traffic query/report path with explicit `captureKind = boundary-failure` instead of silently dropping those requests from local evidence.
- `railix-std-http` now also preserves literal `+` in raw query tokens while still decoding percent-encoded query bytes, and its bounded traffic-read path no longer materializes and sorts the full capture corpus just to satisfy a small `limit`.
- `railix-std-http` now also enforces a fixed first-slice request-body cap during boundary ingestion, rejecting oversized requests with `413 Payload Too Large` before the flow runs and persisting those rejections as boundary-owned HTTP captures.
- `railix-std-http` now no longer clones request bytes a second time in the internal snapshot path, and large successful handled requests now persist only a bounded body preview plus size/truncation metadata instead of always retaining the full structured request payload in `http-capture.json`.
- `railix-std-http` now also exposes `HttpTrafficPanelMain`, a pack-owned local traffic-panel runtime that renders the bundled `http-traffic.panel.yaml` view as a stable table or JSON surface over persisted HTTP capture artifacts instead of leaving the panel deliverable as metadata only.
- `Slice 7: First protocol pack` is now closed: the repo has a real `railix.std.http` pack, a real `HttpTrigger`, a real envelope mapper, a real reply adapter over generic `Reply`, durable sample request capture, and a concrete traffic-panel runtime over those captures, while the roadmap acceptance still remains protocol-generic rather than Creator-UI-specific.
- `railix.std.http` metadata is now aligned to the current runtime surface: `HttpTrigger` advertises `immediate` only, and the bundled traffic-panel metadata now points at `http-capture.json` run artifacts instead of pretending the required `method`/`path` fields already exist durably in generic `runSignals`.
- `Envelope`, `Reply`, and `Patch` invariants are tighter now, and the runtime reply parser fails with explicit boundary diagnostics instead of leaking raw enum/cast errors.
- Standard-pack modules now declare their `railix-kernel` dependency and compile through the reactor build, and `railix-std-data` has moved beyond a marker module into the first honest provider-backed pack slice.
- `railix-kernel` now enforces a hard JaCoCo verification gate in Maven at `95%` line coverage and `90%` branch coverage.
- `KernelContractCodec` now round-trips `AppPlan` itself and exposes a shared stable-JSON parser, so persisted plan artifacts and persisted signal replay now use one deterministic JSON boundary instead of separate parsers drifting apart.
- `railix-kernel` now includes `BuiltRailixAppLoader`, which loads persisted JSON `AppPlan`, `Envelope`, and `SettingsTree` artifacts from filesystem paths or `classpath:` locations.
- `railix-kernel` now includes `BuiltRailixAppMain`, a minimal single-run launcher with `--plan`, `--envelope`, and `--runs-root` plus optional `--settings` and `--run-id`, and that launcher now proves run-folder artifact creation, generated run ids, explicit CLI rejection paths, and failed-run artifact persistence at the public bootstrap boundary.
- `AppPlan.StepInvocation` now carries step-local config as a real kernel-owned runtime field, `KernelContractCodec` round-trips that config while defaulting missing historical `config` and `permissions` plan blocks safely, and `Step.ExecutionInput` now exposes the config surface while preserving the legacy no-config constructor shapes used by the existing pack tests.
- `railix-std-data` now contains a first real `railix.std.data.DataTransform` provider instead of only helper captures: it accepts declarative `config.mappings`, evaluates them sequentially against the evolving `ContextDoc`, and proves real `payload -> ctx` plus `ctx -> reply.*` shaping through installed-pack in-process and named-module launcher boundaries.
- The first real `DataTransform` slice now supports the narrow operator set already exercised by repo examples and tests: `path`, `const`, `trim`, `lower`, `default`, `template`, `toInt`, and `multiply`, with canonical numeric output and deterministic failures for malformed inputs.
- `railix.std.data.DataTransform` now supports one honest repeat/selector slice instead of rejecting it outright: repeat blocks can enumerate concrete selector matches through the public `ContextDoc` SPI, bind one current-item alias plus ancestor-only parent aliases, materialize exactly one repeated `[*]` target slot per nested mapping, and continue sequential patch application across `ctx` and `reply`.
- The repeat slice stays intentionally narrow and explicit: nested repeat blocks still fail, repeated nested targets must contain exactly one `[*]`, alias-rooted path expressions fail clearly when the alias is unknown, and non-`ctx`/`reply` targets still fail before patch emission.
- `railix.std.data.DataValidate` now exists as a first real validation step instead of a manifest-only promise: it accepts step-local `config.required` path lists, returns declared `valid` or `invalid` outcomes based on non-null path resolution against the current context surface, fails malformed config explicitly, and proves real branch behavior through installed-pack in-process and named-module launcher execution.
- `BuiltRailixAppLoader` now also overlays multiple `SettingsTree` artifacts deterministically in caller-supplied order, so launcher-boundary settings precedence can be proven without ad hoc merge code in tests or scripts.
- `BuiltRailixAppMain` now accepts repeatable `--settings` arguments, allowing explicit layered settings loading from both filesystem paths and `classpath:` resources at the public launcher boundary.
- `BuiltRailixAppLoader` now exposes a fixed packaged-settings discovery contract for the launcher: optional packaged defaults from `classpath:/railix/settings/defaults.json` plus explicit packaged profiles from `classpath:/railix/settings/profiles/<name>.json`, with clear failure for invalid or missing requested profiles.
- `BuiltRailixAppMain` now accepts optional `--profile <name>` and auto-loads packaged defaults plus packaged profile defaults before explicit repeated `--settings`, so the launcher now has a small real packaged configuration story instead of only ad hoc file overlays.
- `BuiltRailixAppLoader` now also converts runtime key/value overrides into real `SettingsTree` overlays: prefixed environment variables use `RAILIX_SETTING__<railix__path>` and repeatable CLI assignments use `--set <railix.path=value>`, both materialized as explicit string-valued runtime overrides instead of fake config magic.
- `BuiltRailixAppMain` now overlays launcher settings in documented precedence order: packaged defaults/profile, explicit `--settings` files, environment overrides, then repeatable `--set` overrides, and malformed `--set` input now fails at CLI parsing before plan loading.
- `BuiltRailixAppLoader` now also converts prefixed JVM system properties into real `SettingsTree` overlays: `-Drailix.setting.<railix.path>=<value>` lands between environment overrides and repeatable `--set`, with explicit rejection for malformed property-path shapes.
- `BuiltRailixAppMain` now projects JVM properties into a string-only map, overlays them after environment variables and before `--set`, and keeps profile/override misuse on the usage-failure path instead of degrading into generic runtime noise.
- `railix-std-data` now also proves the launcher boundary through a real child JVM on a named module path: `java --module-path <kernel>:<std-data> --module railix.kernel/dev.nanonative.railix.kernel.runtime.BuiltRailixAppMain` resolves the installed `std.data.capture-payload` provider through `ServiceLoader`, executes successfully, and persists `signals.ndjson` plus `summary.json` without injected resolver wiring.
- `tools/package/jlink-dev.sh` is now a POSIX `sh` packaging smoke script that discovers built project modules from real Maven outputs, builds a modular runtime image with `--bind-services`, and exposes a `railix-app` launcher rooted at `railix.kernel/dev.nanonative.railix.kernel.runtime.BuiltRailixAppMain`.
- `tools/package/jpackage-app-image.sh` is now a POSIX `sh` host-native packaging smoke script that reuses the proven `jlink` runtime image and produces a `jpackage --type app-image` launcher without falling back to the stale non-modular `--main-jar` path.
- `tools/package/jpackage-pkg.sh` is now a POSIX `sh` installer smoke wrapper that reuses the proven app-image path and produces an unsigned local `jpackage --type pkg` artifact on macOS instead of duplicating packaging logic again.
- `tools/package/jlink-dev.sh` now prefers fresh modular `target/classes` outputs over stale jars during dev packaging smoke runs, preventing false proof from yesterday’s launcher code when tests build images in the same Maven invocation.
- `railix-kernel` now ships minimal packaged runtime settings fixtures in main resources at `railix/settings/defaults.json` and `railix/settings/profiles/dev.json`, which are enough to prove packaged-resource discovery through both named-module and packaged-image launches without inventing a builder that does not exist yet.
- `railix-std-data` now contains a second real standard-pack step provider path through `std.data.capture-setting`, which reads `settings.app.mode`, patches context, and persists the resolved winner in reply metadata for packaging-boundary proof.
- `railix-std-data` now has green end-to-end packaging proof for both scripts: the test suite builds a real `jlink` runtime image, builds a host-native `jpackage` app-image from it, launches each packaged binary through `BuiltRailixAppMain`, and asserts persisted `signals.ndjson` plus `summary.json` artifacts for the installed `std.data.capture-payload` provider.
- `railix-std-data` now also proves a host-native macOS installer artifact boundary: the suite builds a real unsigned `.pkg`, asserts the payload contains `RailixII.app` and the packaged launcher/runtime, expands the installer with `pkgutil`, and verifies `/Applications` install metadata without pretending the installer was executed.
- `railix-std-data` now also proves explicit layered settings precedence through the real launcher boundary on all three current surfaces: named-module JVM launch, scripted `jlink` runtime image, and scripted `jpackage` app-image, with the resolved winner persisted in `reply.produced` metadata and `summary.json`.
- `railix-std-data` now also proves launcher-owned key/value runtime overrides through the real public boundary: named-module launch proves environment override ingestion, and scripted `jpackage` app-image launch proves CLI `--set` overrides beat both packaged profile defaults and environment overrides.
- `railix-std-data` now also proves JVM system-property precedence through the real public boundary on JVM-driven launch surfaces: named-module launch proves `-Drailix.setting.settings.app.mode=...` beats packaged profile defaults and environment overrides, and a second named-module run proves `--set` still wins over the same property path.
- `railix-std-data` now also proves native app-image forwarding for the same property path on the current macOS packaging surface: `JAVA_TOOL_OPTIONS='-Drailix.setting.settings.app.mode=...'` reaches the packaged launcher, beats packaged profile defaults plus env overrides, and still loses to explicit `--set` as expected.
- `railix-std-data` now also proves automatic packaged defaults, packaged profile defaults, and explicit file overrides through the real launcher boundary across the named-module JVM, scripted `jlink` image, and scripted `jpackage` app-image surfaces.
- `tools/package/jlink-dev.sh` now accepts builder-owned packaged settings through `APP_SETTINGS_DIR`, rejects empty/malformed builder settings directories explicitly, and makes those staged assets authoritative instead of silently falling back to the kernel placeholder defaults.
- `railix-std-data` now also proves builder-emitted packaged settings through the real packaging boundary on all three current script surfaces: scripted `jlink`, scripted `jpackage --type app-image`, and the macOS `jpackage --type pkg` wrapper all produce runnable launchers that resolve builder-owned packaged defaults or profiles instead of the kernel placeholder resources.
- `tools/package/railix-package.sh` now exists as a Railix-owned packaging driver that stages packaged defaults and packaged profile assets from explicit command arguments, rejects malformed profile mappings, and dispatches to the proven `jlink`, `app-image`, and macOS-local `pkg` packaging scripts without exposing `APP_SETTINGS_DIR` as a user-facing contract.
- `tools/railix.sh` now exposes the first honest `railix package` command surface in the repo, keeping the public verb aligned with the docs while remaining a thin wrapper over the current packaging driver rather than pretending a full spec-driven build system already exists.
- `tools/package/railix-package.sh` now also supports an opt-in success-only JSON report through `--report`, emitting the current proven artifact relationship only: absolute `destDir`, type-specific artifact paths, launcher path, and narrow embedded-settings evidence (`defaultsEmbedded` plus packaged profile names).
- `tools/package/railix-package.sh` now defaults omitted `--type` to `app-image`, which is the current primary honest packaging contract, and the suite now proves both that default path and the no-fake-report guarantee for both preflight validation failure and downstream packager failure after dispatch.
- `tools/package/railix-package.sh` now also owns the smallest honest `railix.app.yaml` packaging slice: it reads `app.name`, `app.profiles`, and `app.settings.defaults`, maps `--profile <name>` onto the existing packaged JSON resource convention, and fails explicitly for undeclared app profiles or missing spec-owned defaults files instead of falling through to raw staging errors.
- `tools/package/railix-package.sh` now also packages declared app profiles automatically when `--app` is used without a single selected build profile, so packaged launchers can resolve those shipped profiles later through the existing runtime `--profile <name>` boundary instead of requiring manual `--packaged-profile` wiring for each one.
- `tools/package/railix-package.sh` now also derives `APP_MODULES` from `app.dependencies[*].id` when `--app` is used, validates that each declared module is actually available and built before dispatching to `jlink`, and fails explicitly for undeployable dependencies instead of silently packaging the old fixed module set.
- Public kernel tests now cover 425 passing scenarios across 25 test files, including packaged-default auto-load, packaged profile selection, invalid profile rejection, duplicate `--set` rejection, environment-variable override ingestion, JVM property override ingestion, malformed property-path rejection, malformed `--set` rejection, deterministic multi-settings overlay at the launcher boundary, step-owned audit emission, blank audit-event rejection, retry-time audit re-emission, execution-input audit-scope fallback, and the Step Contract 1.0 connection/configuration/output boundaries in addition to the existing bootstrap, retry, cleanup, artifact-replay, and failure-summary contracts.
- `railix-std-data` now has 66 passing scenarios across 2 test files, covering `DataTransform` provider ownership, repeat/selector expansion, `DataValidate` required-path validation with explicit malformed-config rejection, `DataRoute` ordered branch routing, `DataForEach` loop-state cursor preparation with reserved-alias and unsafe-state-path rejection, `DataAggregate` selector-driven `count|sum|min|max|collect` aggregation with empty-branch and numeric-coercion proof, installed-pack valid/invalid branch routing, named-module launcher execution, scripted `jlink` runtime-image launch, scripted `jpackage` app-image launch, local macOS `.pkg` artifact inspection, environment-variable override proof, JVM system-property override proof, native app-image `JAVA_TOOL_OPTIONS` forwarding proof, CLI `--set` precedence proof, builder-emitted packaged-settings proof, explicit malformed `APP_SETTINGS_DIR` rejection, `railix package` proof for explicit `jlink`, explicit `app-image`, default `app-image`, app-driven `app-image`, app-driven `jlink`, app-driven automatic multi-profile packaging, declared-profile auto-package failure proof, dependency-aware app-driven module selection, undeployable app-dependency rejection, and success-only report guarantees through the real `BuiltRailixAppMain` public entrypoint.
- `railix-std-trigger` now has 5 passing scenarios across 2 test files, covering provider ownership, manual-trigger context normalization, CLI-trigger reply-channel normalization, unknown-use rejection, in-process installed-pack execution, and named-module launcher execution through the real `BuiltRailixAppMain` public entrypoint.
- `railix-std-store` now has 18 passing scenarios across 2 test files, covering read/write/delete/exists/list provider ownership, explicit `null` round-trip, ctx-over-payload precedence, invalid root/key diagnostics, corrupt on-disk JSON failure, unknown-use rejection, in-process installed-pack execution, named-module launcher execution, and scripted `jlink` launcher execution through the real `BuiltRailixAppMain` public entrypoint.
- `railix-std-metrics` now has 8 passing scenarios across 2 test files, covering provider ownership, payload-driven emission, ctx-over-payload precedence, missing/non-numeric value rejection, blank-label rejection, undeclared metric rejection, undeclared-label rejection, duplicate-descriptor rejection, in-process installed-pack execution, and named-module launcher execution through the real `BuiltRailixAppMain` public entrypoint.
- `railix-std-audit` now has 9 passing scenarios across 2 test files, covering provider ownership, payload-driven emission, ctx-over-payload precedence, missing-event rejection, blank-event rejection, missing-data defaulting, non-object data rejection, unknown-use rejection, in-process installed-pack execution, and named-module launcher execution through the real `BuiltRailixAppMain` public entrypoint.
- `railix-std-file` now has 20 passing tests across 2 test files, covering provider ownership, payload-driven write, ctx-over-payload precedence, traversal rejection, symlink escape rejection, current-run relative-ref provenance success/failure, metadata mismatch rejection, read miss, out-of-root file-ref rejection, delete-by-ref success, unknown-use rejection, in-process installed-pack execution, and named-module launcher execution through the real `BuiltRailixAppMain` public entrypoint.
- `railix-std-http` now has 38 passing scenarios across 8 test files, covering provider ownership, envelope mapping, reply adaptation, handled request capture, rejected pre-run request capture, oversized-request rejection at the real HTTP boundary, exact-cap acceptance, large handled-request preview-only capture, unexpected boundary-failure capture, stable-JSON traffic-report CLI usage/failure paths, table/JSON traffic-panel rendering, newest-first bounded traffic reads, unknown capture-kind rejection, and raw-query `+`/percent-decoding preservation through the real HTTP boundary and local report entrypoints.
- The current kernel verify report is now `96.59%` line coverage and `90.76%` branch coverage for `railix-kernel`.
- `railix-creator` now owns a real user-visible browser surface: bundled HTML/CSS/JS served by `CreatorShellServer`, artifact-backed pack/run/signal inspection from `CreatorWorkspaceSnapshot`, a read-only HTTP traffic panel over persisted std-http captures, a bounded Creator-owned live session view over `/api/control/sessions` with per-session structured timeline plus context-patch summaries, bounded metric and audit evidence for the preferred run source, a matching newest-run artifact detail view in `/api/workspace`, a separate bounded historical persisted-run drill-in surface over `/api/runs/detail`, in-band invalid-summary markers on run rows plus selected-run detail, and a real built-app launch form that executes explicit persisted `plan.json` / `envelope.json` inputs plus supported `railix.app.yaml` / envelope YAML inputs through the public `BuiltRailixAppMain` boundary rather than a fake app-spec runtime.

## Current blockers

- No remaining blocking Slice 1 gaps are known after the current verification pass.
- `Slice 9: Multi-instance foundation` is now closed as a first honest foundation milestone: shared registry discovery exists, registry heartbeats advertise installed StepProvider capability plus supported resource-ref patterns, each live instance publishes explicit loopback-only trust, workspace/runtime alignment, local-only execution semantics, an explicit distributed-support rejection matrix, live Creator load, and raw host runtime identity, and the current boundary now says plainly what distributed behavior still does not exist instead of leaving it implied.
- `Slice 10: Remote execution boundary` now has nine honest seams: provider-backed runtime identity exists through the shared instance registry, a concrete deterministic request-validation boundary exists through `POST /api/remote-execution/requests`, accepted same-instance inline execution now exists for both plain local payloads and resource-backed/runtime-permissioned local steps when the request carries caller-supplied granted permissions plus a target-local settings source, accepted same-instance deferred execution now exists through a bounded local queue with persisted lifecycle evidence and explicit overload rejection, unsupported timeout propagation is now rejected explicitly for every non-zero value instead of degrading into silent ignore behavior, resolver/catalog drift is now rejected explicitly instead of degrading into a late request-path `500`, discovered peer instances are now distinguished from truly unknown targets with explicit target-side diagnostics and blocker codes, a dedicated `POST /api/remote-execution/preflight` admission contract now exposes the same request semantics without execution side effects and now proves its inactive/incomplete/digest-mismatch/use-mismatch/resource-mismatch blocker matrix through the public shell boundary, and a bounded artifact-backed remote-request lifecycle/report surface exists through `GET /api/remote-execution/requests`; but honest runtime pack identity still does not exist, there is still no cross-instance trust story, no cross-instance permission propagation story, no distributed/shared queue story, no shared cache-digest story, and the request path still lacks the same public-boundary parity for those discovered-peer blocker branches.
- Cross-slice limitations remain: the Creator now consumes local run artifacts and std-http traffic evidence through a real browser shell, can launch persisted built-app inputs plus a narrow supported `railix.app.yaml` / envelope YAML subset through the public launcher boundary, owns a bounded live control surface for Creator-started sessions with structured timeline, context-patch, metric, and thin audit activity, supports direct persisted historical run drill-in by exact run identity, degrades malformed `summary.json` artifacts in-band instead of crashing workspace discovery, owns a raw committed authoring-spec edit/save boundary under `examples/`, owns a committed-example export boundary that writes persisted JSON runtime pairs under the same example tree and immediately reuses the existing launcher/discovery path, owns a narrow create boundary that scaffolds one new example directory with one runnable `railix.app.yaml` plus one sample envelope under `examples/`, and now also launches from a packaged app-image on the current host through the public `railix package --mode creator` path, but there is still no external app attach boundary, no multi-flow authoring runtime shape, invalid or incomplete example specs remain blocked explicitly, packaging proof is still limited to dev-time `jlink`, host-native `jpackage --type app-image`, local macOS unsigned `.pkg` artifact smoke on the current machine, and a narrow string-valued launcher override surface rather than a full typed packaging/build pipeline.
- The current local runner still does not justify a blanket no-leak claim: emitted signals are no longer rooted in memory across the returned run record lifetime, bounded read windows now exist, and Creator live-session launcher capture is now bounded at source with explicit retained-size/truncation metadata, but the deprecated `RunSignalLog.signals()` compatibility shim still rematerializes full history if called, packaged-runtime proof is still only short-lived smoke rather than soak or leak instrumentation evidence, and unmanaged resources outside the kernel registry can still leak.
- Packaging is not production-ready yet: there is still no cross-host installer/distribution matrix (`msi`/`deb`/`rpm` plus non-local `pkg` proof), the new `railix package` surface still owns only a narrow `railix.app.yaml` subset (`app.name`, direct dependency module ids, packaged profile names, packaged defaults metadata) rather than dependency resolution/lockfile generation or a typed settings-conversion pipeline, launcher key/value overrides are still string-only, native property forwarding is currently proven only through `JAVA_TOOL_OPTIONS` on the current macOS app-image surface rather than generic direct `-D` argv forwarding or full pkg-install execution, there is no signing/notarization path, there is still no automated installer execution smoke beyond local artifact inspection, packaged Creator launch still needs explicit `--repo-root` instead of one-click workspace discovery, and there is still no leak/soak evidence for long-lived packaged runtime sessions.
- `Slice 6: Standard packs` is now closed as a first-slice milestone: `DataTransform`, `DataValidate`, `DataRoute`, `DataForEach`, and `DataAggregate` all have real installed-pack execution proof through in-process and named-module public boundaries, while broader DataWorkbench expressiveness remains future Slice 4 work rather than hidden incomplete scope inside Slice 6.
- `railix-std-data` is no longer only a packaging/helper slice: `railix.std.data.DataTransform` now runs through step-local config, installed-pack execution, named-module launcher proof, and repeat/selector expansion with alias-scoped parent binding; `railix.std.data.DataValidate` now provides real valid/invalid routing for required path presence; `railix.std.data.DataForEach` now provides a real plan-loop cursor slice with guarded state ownership plus alias-safe parent binding; `railix.std.data.DataAggregate` now provides a real selector-driven aggregate slice for `count|sum|min|max|collect` into `ctx`; and `std.data.capture-setting` remains an intentionally narrow settings-boundary proof helper rather than a generic secret copier.
- `Slice 3: Creator minimum` is now closed as a first-slice milestone: there is a real static-assets source tree, a real localhost browser/runtime surface owned by `railix-creator`, a real user-visible screen for pack/run/signal/HTTP-capture inspection, launch-prep discovery for runnable persisted inputs plus supported authoring-spec YAML pairs versus blocked invalid/package evidence, a bounded Creator-owned live session view for persisted built-app runs with structured timeline and context-patch summaries, explicit accepted-then-failed proof for that same live-session boundary, bounded metric and thin audit evidence for the preferred run source, a matching newest-run artifact view in `/api/workspace`, a bounded historical persisted-run drill-in surface selected from the existing runs list, raw-signal and terminal-failure parity for historical persisted runs, auto-refresh of the selected historical detail surface, a cursor-paged historical persisted-run query surface with exact-match filters, an exact total-matched count surface for the current historical query, a read-only run-stage graph activity surface derived from bounded signal evidence, explicit `summaryReadStatus=missing` discovery for persisted signal-only run folders, in-band invalid/missing summary status for discovered runs and selected details, a real launch form for explicit persisted `plan.json` / `envelope.json` runs plus supported `railix.app.yaml` / envelope YAML pairs, a raw committed authoring-spec edit/save panel for discovered example specs, and launcher-owned bounded output retention with preserved size/truncation metadata. The remaining gaps are now correctly future-slice concerns: there is still no external live attach boundary, no editable flow graph, and no richer free-text or saved-query history surface.
- `railix-std-store` is still only a first slice: `store.read` / `store.write` now execute through kernel-owned runtime permission decisions for canonical `store/<normalized-key>` resources with plan-granted wildcard support, but there is still no metadata sidecar contract, no expiry behavior, no cross-process locking, no richer prefix-grant surface beyond terminal `/*` matching, and no non-filesystem backend proof.
- `railix-std-metrics` is still only a first slice: there is only one declared descriptor (`railix.metric.value`), no dynamic descriptor catalog, no aggregation/export path, no panel/query surface, and no distributed metrics story.
- `railix-std-audit` is still only a first slice: event names are still unconstrained strings, payloads are object-only, there is no audit-definition catalog on `StepContract`, and there is no audit viewer or query/panel proof yet.
- `railix-std-file` is still only a first slice: `resource.create` / `resource.read` / `resource.write` now emit kernel-owned permission decisions and require explicit plan grants for canonical `file/<normalized-relative-path>` resources before filesystem access; emitted `FileRef.path` values and `resource.created` metadata paths are now normalized root-relative paths, relative refs now require same-run kernel provenance plus metadata/path/digest/size agreement before read/delete, legacy absolute refs that stay under `settings.file.rootDir` remain as the explicit compatibility fallback, write/read/delete reject symlink traversal through configured-root ancestors and symlinked leaf files instead of relying on lexical containment alone, and durable file writes use a kernel-owned provenance-only resource registration path instead of retaining no-op closeables until run teardown just to get `refId` plus `resource.created`, but operations are still stable-JSON only, path access is limited to a configured root, writes still persist durable files rather than run-scoped temporary handles, there is still no cross-run file-ref capability story, and there is still no streaming or large-file proof.
- `railix-std-http` is now a complete first protocol-pack slice, but its scoped limits remain explicit: the current boundary is synchronous and immediate-only, request bodies are intentionally limited to JSON objects, `Reply` modes beyond `NONE` and `IMMEDIATE` fail explicitly with `501`, `settings.http.tls.enabled=true` is rejected, the new Creator UI traffic view is artifact-backed only rather than live-streamed from a control endpoint, boundary-owned rejection/failure captures still are not kernel run artifacts, request bodies are still fully buffered in memory up to the fixed first-slice cap before JSON decode, there is still no streaming/session/deferred response handoff, there is still no auth model, and there is still no long-lived server soak evidence.
- `KernelContractCodec` remains the largest internal hotspot by raw branch count, but it is now above the enforced module gate and no longer blocks Slice 1 closure.

## Roadmap status

| Item | Status | Complete | Left |
|---|---|---:|---:|
| `Step Contract 1.0 and Connection Semantics` (`Slice 1` closure) | Done | 100% | 0% |
| `Slice 1: Kernel contracts` | Done | 100% | 0% |
| `Slice 2: Local execution kernel` | Done | 100% | 0% |
| `Slice 3: Creator minimum` | Done | 100% | 0% |
| `Slice 4: DataWorkbench` | Planned | 0% | 100% |
| `Slice 5: Packaging` | Done | 100% | 0% |
| `Slice 6: Standard packs` | Done | 100% | 0% |
| `Slice 7: First protocol pack` | Done | 100% | 0% |
| `Slice 8: Build app as internal tool` | Done | 100% | 0% |
| `Slice 9: Multi-instance foundation` | Done | 100% | 0% |
| `Slice 10: Remote execution boundary` | In Progress | 92% | 8% |
| `Slice 11: Production hardening and DX gates` | Planned | 0% | 100% |
| `Slice 12: Step sandboxing and resource metering` | Planned | 0% | 100% |
| `Slice 13: Distributed Step orchestration` | Planned | 0% | 100% |
| `Slice 14: Step-owned state, storage, sharding, and replication` | Planned | 0% | 100% |
| `Slice 15: Canonical JSON, XML, and YAML interchange` | Planned | 0% | 100% |
| `Slice 16: Compiler, dependency graph, and release supply chain` | Planned | 0% | 100% |

## Overall roadmap estimate

- Accepted-slice completion index: `55.8%`
- Estimated left to integrate: `44.2%`
- Formula: `(8 completed slices * 100 + Slice 10 at 92) / 16 tracked top-level slices = 55.75%`, rounded to one decimal place. The Step Contract closure item is evidence inside Slice 1 and is not double-counted.
- This replaces the prior `95%` estimate because sandboxing/metering, distributed orchestration, state/sharding/replication, canonical XML interchange, and compiler/release supply-chain work were omitted from its denominator. The new number is intentionally conservative and changes only when a top-level slice moves against acceptance evidence.

## Agent-assisted findings used in this pass

- `Kepler`: current HTTP exploration confirmed the smallest honest `Slice 7` increment was a real synchronous protocol boundary using JDK `HttpServer` plus generic `Envelope`/`Reply`, not metadata or mock transport theater.
- `Herschel`: current runtime review rejected fake stream/session/deferred support because the kernel still closes run-scoped resources before returning; the landed slice stays immediate-only and says so plainly.
- `Darwin`: current test review forced negative protocol cases alongside the happy path, including unsupported reply modes, invalid status/header metadata, request content-type rejection, and invalid request-body shape.
- `Ampere`: implementation branch exploration overlapped with the final module shape, but the landed path intentionally kept the narrower `EmbeddedHttpBoundary` design and deleted the redundant alternate server files instead of merging duplicate surfaces.
- `Galileo`: current follow-up exploration confirmed the next honest `Slice 7` increment was sample request capture over real local artifacts, not a fake traffic panel runtime, and reinforced that the existing generic panel surface is still docs-only.
- `Singer`: current follow-up review forced the missing persistence gap into the acceptance surface: durable request evidence now exists for handled runs, while rejected pre-run requests remain an explicit open gap instead of disappearing behind a hopeful panel YAML.
- `Godel`: current follow-up lifecycle review forced two safety constraints into the landed shape: HTTP run ids now include wall-clock uniqueness to avoid stale run-folder reuse on boundary restarts, and `http-capture.json` now publishes atomically through temp-file move semantics instead of plain overwrite.
- `Arendt`: current rejection-capture review argued for a separate pre-run rejection artifact contract and a distinct report surface from the handled-run query; the landed pass kept a shared capture filename for reuse but made the report surface explicit and keeps `captureKind` visible so the boundary does not silently flatten rejections into success-looking rows.
- `Schrodinger`: current boundary-test review forced the new HTTP pass to cover the exact mapper-rejection path at the public boundary, plus mixed handled/rejected query/report behavior, instead of stopping at mapper-only unit tests.
- `Gauss`: current exploration confirmed the next honest Slice 7 increment was a public traffic-report entrypoint over persisted capture artifacts, not a fake Creator panel runtime, and pushed the acceptance shape toward real CLI/runtime consumption.
- `Bohr`: current HTTP review found a real observability hole where unexpected boundary `500`s could disappear from traffic evidence entirely, plus a raw-query correctness bug around literal `+`; both are now fixed and covered at the public boundary.
- `Chandrasekhar`: current runtime review flagged the old full-sort query path as an artifact-count-sized memory spike for small `limit` reads, which is why the landed traffic reader now keeps only the newest bounded window in memory while leaving request-body buffering and payload-retention hardening explicitly open.
- `Zeno`: current Creator query review caught one surrounding production bug and one honesty gap while the `/api/runs` slice was in flight: the historical query surface needed to tolerate a missing runs root instead of blanking the shell after `/api/workspace` succeeded, and the visible count label needed to describe bounded query results rather than implying a total/latest-run count. The landed pass fixes both without pretending the broader Creator milestone is done.
- `Chandrasekhar`: current Creator history review forced the new paging surface onto a deterministic newest-first tuple order instead of timestamp-only luck, and it highlighted two real UX/runtime honesty gaps that the landed pass closes: cursor/filter mismatch now fails explicitly, and auto-refresh now replays the already loaded pages instead of snapping paged history back to page 1.
- `Kant`: current Creator history/count review correctly rejected free-text search theater as the next step, and it forced this pass to expose the already computed exact matched-row count honestly in the UI and progress ledger instead of continuing to describe page-local rows as if they were the whole filtered result set.
- `Avicenna`: current Creator graph review correctly pushed the slice away from fake plan editing or attach mythology. The landed pass stays on fixed run-stage graph activity derived from real bounded signals and continues to describe the graph shell as read-only evidence, not as a generic editable flow runtime.
- `Parfit`: current Creator exploration confirmed the next useful read-only Slice 3 debugging increment would be historical raw-signal and terminal-failure parity on `/api/runs/detail`, not another fake attach story.
- `Erdos`: current runtime review found a real Creator memory-spike risk in full live-session `stdout` / `stderr` buffering before preview truncation, which is now called out explicitly as a remaining no-leak blocker instead of being hidden behind the existing bounded UI preview.
- `Einstein`: current correctness review found no new shipped regression, but it highlighted the missing accepted-then-failed live-session public-boundary scenario as the highest-value negative Creator test still absent from the current suite.
- `Nash`: current test review pushed the historical parity slice to prove three concrete cases instead of one vague promise: clean historical raw-signal preview, historical terminal-failure summary projection, and partial historical signal degradation.
- `Volta`: current runtime review confirmed the historical parity slice is bounded and honest, and it caught one real UX/runtime edge that is now fixed in the landed pass: selected historical detail must refresh during auto-refresh instead of freezing after the first click.
- `Curie`: current DX review pushed back on turning request-size hardening into a new public settings promise, which is why the landed size limit stays a fixed first-slice guardrail instead of advertising `settings.http.maxRequestBytes` before the protocol pack earns a broader tuning contract.
- `Darwin`: current Creator exploration confirmed the next honest `Slice 3` increment was a read-only HTTP traffic slice over persisted `http-capture.json` artifacts through the existing `/api/workspace` boundary, not a fake live attach or control-endpoint story.
- `Euclid`: current Creator/runtime review confirmed the repo still has no honest `railix.app.yaml -> plan.json` runtime bridge, which is why the new Creator launch surface stays explicitly on persisted built-app `plan.json` and `envelope.json` inputs through `BuiltRailixAppMain` instead of pretending arbitrary authoring specs are runnable.
- `Helmholtz`: current YAML-surface review narrowed the supported bridge contract to the example subset that already exists in the repo, and it caught two honesty hazards early: lowercase reply-channel modes need normalization, and incomplete examples like `order-approval` must stay blocked instead of being marked runnable.
- `Goodall`: current Slice 9 review rejected my first pack-advertisement instinct because `repoRoot/packs` is workspace metadata rather than installed runtime-provider identity. The landed capability payload stays smaller and honest: `load` plus raw host `runtime`, with pack identity left for a later slice that first earns a real runtime contract.
- `Dirac`: current Slice 9 follow-up review confirmed the next honest seam was a runtime provider/use-id catalog rather than repo pack scan, and it forced one truthfulness fix into the landed shape: providers that do not report `supportedUses()` are now surfaced as `unreported` instead of being silently counted as supporting zero uses.
- `Bernoulli`: current closure review confirmed Slice 9 can close now that the shared registry contract publishes an explicit distributed-support rejection matrix, and it recommended splitting the remaining work into `Slice 10: Remote execution boundary` plus `Slice 11: Production hardening and DX gates` instead of inflating Slice 9 or pretending overall 100%.
- `Aristotle`: current Slice 10 review rejected my first “runtime pack identity” wording because the repo can only prove provider-backed runtime identity today. The landed shape keeps `runtimePackIdentitySupported=false`, publishes provider-backed identity separately, and leaves actual pack equivalence for a later contract that first earns artifact/version/digest truth.
- `Boyle`: current Slice 10 contract review pushed the next honest boundary to a real request-validation surface instead of more metadata theater. The landed shape uses `POST /api/remote-execution/requests`, request/response contract version `1`, deterministic rejection codes, and an explicit `not-executed` unsupported result instead of pretending queueing or remote completion already exists.
- `Aquinas`: current deferred-queue review caught three real honesty holes after the first deferred pass: advertised queue capacity was unenforced, `timeoutMillis` was silently ignored, and concurrent instance-registry refreshes could surface request-path `500`s under load. The landed pass now enforces bounded queue admission, rejects unsupported timeouts explicitly, and publishes registry heartbeats through unique temp files so overload stays deterministic.
- `Ampere`: current follow-up review confirmed the cleanest regression hook for provider/execution drift was a direct `CreatorRemoteExecutionBoundary` test with an injected resolver, not a contrived `ServiceLoader` or classloader stunt at the HTTP shell boundary. The landed pass now snapshots installed providers once, shares that snapshot across catalog and resolver wiring, and keeps a narrow direct regression that fails if the boundary ever reintroduces a fresh execution resolver path.
- `Plato`: current follow-up review ranked the smallest honest first cross-instance seam as admission/preflight rather than execution. The landed path now includes the dedicated `POST /api/remote-execution/preflight` contract, so the next honest step is to prove the full discovered-peer blocker matrix through that public boundary instead of leaving inactive/incomplete/mismatch cases covered only indirectly.
- `Meitner`: current review caught one real contract bug and one real proof gap. The landed follow-up now rejects any non-zero `timeoutMillis` value on both request and preflight paths, and it keeps the next Slice 10 task focused on proving the remaining discovered-peer rejection matrix at the public shell boundary rather than pretending those branches are already fully covered.
- `Averroes`: current discovery review correctly ranked the cheapest missing peer proofs in boundary order: stale/inactive first, then target digest mismatch, then step/resource capability mismatches. The landed pass used that ordering to seed deterministic peer fixtures and prove the full discovered-peer preflight blocker matrix without spinning unnecessary extra Creator instances.
- `Pauli`: current read-only export review confirmed the smallest honest next increment was a dedicated `POST /api/authoring-spec/export` boundary that writes exactly one persisted JSON plan/envelope pair under the same example tree, and that the existing runnable-input scan would already discover that pair without a second launcher path.
- `Sagan`: current read-only create review confirmed the smallest honest closure step for Slice 8 was a dedicated create boundary that scaffolds one example-scoped spec plus one sibling sample envelope, and it forced the UI copy to stay explicit that this is not a general compiler or free-form spec generator.
- `Ramanujan`: current Creator-surface review confirmed the next honest post-bridge increment is raw discovered-spec ownership, not metadata-only save theater, and it scoped the remaining follow-up to export/create above that new edit/save surface.
- `Zeno`: current closure review confirmed Slice 7 acceptance was already met by the shipped pack metadata, real HTTP boundary, durable capture/query/report surfaces, and generic `Reply` adaptation; the remaining concerns were real future-slice hardening items, not honest reasons to keep the milestone open.
- `Pathfinder`: confirmed Slice 1 gaps around `Shape`, `Selector`, stable wire contracts, and spec parity.
- `Gatekeeper Reviewer`: confirmed the correct test boundary for this slice is contract-level kernel APIs, not fake app-level E2E.
- `Pathfinder` follow-up: confirmed the highest remaining acceptance impact is `RunSignal`, `PermissionSet`, then `StepContract` and `SettingsTree`.
- `Gatekeeper Reviewer` follow-up: prioritized missing tests for `StepContract`, `RailixValue`, `PermissionSet`, `SettingsTree`, and `RunSignal`, which are now present.
- `Pathfinder` current pass: confirmed the remaining high-value contract work was `Patch` expr parity and stricter `RailixPath` validation.
- `Gatekeeper Reviewer` current pass: pushed for `Patch` and constructor-level `RailixPath` tests in the same pass, which are now present.
- `Pathfinder` latest pass: confirmed `SettingsTree` behavior rules were still only documentation and needed a kernel-owned API boundary.
- `Lagrange`: confirmed the canonical secret-read audit rule should remain a single `setting.read` signal, not a duplicate `audit.emitted` linkage.
- `Gibbs`: confirmed the smallest honest Slice 1 wire-proof boundary is the kernel contract/UI-model layer, which is now backed by `KernelContractCodec` and contract tests.
- `Gatekeeper Reviewer`: caught two real boundary issues in the new codec, which are now fixed: empty YAML collections no longer degrade to `null`, and secret materialized settings are rejected at the UI-model boundary instead of leaking plain text.
- `Pathfinder`: ranked the next honest coverage gains as codec subtype round-trips first, then remaining `SettingsTree` and `Selector` public branches, which guided this pass.
- `Gatekeeper Reviewer` latest pass: confirmed the remaining care point is honesty in how codec coverage is described, especially keeping structural round-trips separate from runtime semantics.
- `Lorentz`: current pass confirmed the highest-yield kernel gaps were codec shape round-trips, malformed nested UI-model branches, `RailixPath` parser guards, and `SettingsTree` normalization rules.
- `Parfit`: current pass challenged overconfident Slice 1 maturity claims around `SettingsTree`, `Shape`, and `RailixPath`, which forced this pass to land evidence before moving the milestone.
- `Godel`: current pass found three real Slice 1 correctness risks by inspection. Two were fixed in this pass: serializer control-character/key safety and out-of-range integer truncation. The third raw-cast finding for `Shape.RefShape` was already resolved locally before the agent result arrived.
- `Pathfinder`: current standard-pack pass forced the permission model to move from decorative contract metadata to a concrete kernel-owned runtime gate with exact-or-terminal-`/*` matching and explicit plan grants for `file/*` / `store/*`.
- `Gatekeeper Reviewer`: current standard-pack pass forced negative proof, not just happy-path logging, so installed file/store pack tests now fail without plan permissions and succeed only with explicit grants.
- `Rootkeeper`: current standard-pack pass constrained the slice to preflight authorization rather than a new long-lived provenance registry, keeping runtime ownership bounded while leaving path-redaction and forged-ref hardening explicitly out of scope.
- `Kuhn`: defined the smallest honest Slice 2 boundary as a real `LocalExecutionKernel.run(...)` path with concrete context handling instead of fake orchestration glue.
- `Hilbert`: forced tighter `Reply`, `Envelope`, and `Patch` invariants plus the missing runtime acceptance cases around reply mode enforcement, undeclared outcomes, and patch semantics.
- `Beauvoir`: caught the identity-comparison bug that made context-derived replies dead code and flagged the missing failed-run artifact handling for resolver and final reply failures.
- `Noether`: constrained the next honest Slice 2 increment to runtime settings reads with evidence-backed roadmap movement only after green verification.
- `Averroes`: pushed the design toward a kernel-owned settings facade with sanitized permission decisions, which prevented raw settings state and ad hoc audit emission from leaking into step code.
- `Zeno`: constrained the next honest Slice 2 increment to retry policy plus real attempt accounting, and prevented roadmap inflation beyond what green runtime evidence supports.
- `Einstein`: forced the retry work to include attempt propagation, patch-failure boundaries, and settings-facade correlation instead of layering retries onto broken accounting.
- `Helmholtz`: constrained the next honest post-retry increment to kernel-owned retry metrics before broader runtime API expansion, which is now backed by green public-boundary tests.
- `Cicero`: challenged the no-leak target directly and confirmed the next blocking Slice 2 gaps are resource lifecycle ownership and signal-retention strategy, not more roadmap theater.
- `Huygens`: constrained the resource lifecycle increment to three public-boundary proofs only: successful cleanup, late terminal failure cleanup, and retry cleanup before the next attempt.
- `Mencius`: forced the implementation shape toward a kernel-owned resource scope on `Step.ExecutionInput`, with attempt-local promotion to run scope and deterministic reverse-order cleanup.
- `Aquinas`: confirmed the signal-retention slice is materially fixed for run-record lifetime memory ownership, while flagging the remaining bounded-read and unmanaged-resource gaps that still block any no-leak claim.
- `Archimedes`: challenged the signal-artifact path on replay and summary truthfulness, which led to new regression coverage for malformed persisted signals, decimal-number replay, and failed-run summary counts before the milestone moved.
- `Banach`: argued for a method-owned bounded replay surface instead of exposing caller-owned streams, which guided the `readSignals(offset, limit)` plus `forEachSignal(...)` shape.
- `Hooke`: challenged the hard removal of `signals()` on compatibility grounds, which led to keeping it only as a deprecated shim while moving the test and acceptance surface onto bounded reads.
- `Kant`: confirmed the cleanup-failure gap had shifted from raw ownership to public artifact honesty, especially the late terminal path that can emit `reply.produced` before a failed outcome.
- `Ohm`: forced the cleanup-failure slice to include explicit throwing-close acceptance tests instead of counting successful cleanup tests as evidence they never provided.
- `Fermat`: confirmed the smallest honest harmonization was to keep `reply.produced` as a success-only artifact emitted after run-scope cleanup, and to leave richer terminal failure diagnostics as a separate follow-up slice.
- `Pathfinder`: current pass confirmed the correct pack-backed kernel slice is a provider SPI behind `StepResolver`, not pack semantics pushed down into the kernel loop, and flagged launcher/bootstrap wiring plus duplicate-provider rejection as the remaining honesty checks.
- `Rootkeeper`: current pass pushed terminal cleanup diagnostics toward a persisted `summary.json` artifact with `phase` plus suppressed failure detail instead of a new run-level signal, which is now the landed shape.
- `Gauss`: current pass constrained the bootstrap slice to a minimal single-run launcher contract, JSON-only persisted plan/envelope/settings artifacts, and an explicit list of packaging claims that still remain false until default installed-pack launcher proof exists.
- `Socrates`: current pass confirmed the honest remaining proof surface is an exploded named-module launcher E2E owned by `railix-std-data`, and that jar/app-image scripts remain packaging work rather than valid acceptance evidence for the current kernel slice.
- `Locke`: current pass confirmed the honest packaging proof must be a prebuilt modular runtime image with `--bind-services` and a real launcher, which is now the shape exercised by the scripted `jlink` and `jpackage` app-image tests.
- `Dirac`: current pass recommended keeping the next launcher/settings increment narrow and evidence-first; the landed shape is even smaller than the suggestion, using repeatable `--settings` instead of a new `--profile` flag while still proving layered precedence.
- `Wegener`: verification pass caught three real defects before this milestone moved: incorrect handcrafted settings UI-model resources, stale-jar bias in dev packaging smoke, and a bad `PermissionSet` constructor call in the new standard-pack step.
- `Copernicus`: current pass pushed the launcher contract toward fixed packaged resource paths plus an explicit `--profile` selector, and reinforced that missing requested profiles must fail loudly instead of silently degrading into empty settings.
- `Pasteur`: current pass constrained the next honest packaging increment to a thin `jpackage --type pkg` wrapper plus macOS-only artifact inspection through `pkgutil`, explicitly rejecting fake installer-success claims.
- `Carson`: current pass found one real DX regression in the new launcher override slice: CLI-originated configuration errors were splitting between usage failures and generic runtime failures. The launcher now rejects malformed profile values, missing packaged profiles, and duplicate `--set` paths at the CLI boundary, and the progress doc has been realigned to the implemented surface.
- `Kepler`: current pass confirmed the smallest remaining runtime-override increment was the already-reserved JVM property slot, recommended the `-Drailix.setting.<railix.path>=<value>` syntax, and explicitly separated JVM-driven proof from native launcher forwarding claims.
- `Euler`: current pass constrained the native-forwarding question to one honest claim only: prove what the packaged launcher actually honors on this host, or document the limitation. The resulting evidence shows current macOS app-image forwarding works through `JAVA_TOOL_OPTIONS`, not through direct native `-D` argv.
- `Turing`: current pass reviewed the builder-emitted packaged-settings slice, validated the staged-resource approach, and correctly called out the two remaining honesty gaps that were then closed here: empty `APP_SETTINGS_DIR` now fails explicitly, and the macOS `pkg` wrapper now has builder-backed runtime proof instead of only indirect inference.
- `Heisenberg`: current pass constrained the next DX increment to the smallest honest public packaging verb already implied by the repo: `railix package`, with `app-image` as the primary real contract, `pkg` as a macOS-local artifact caveat, and no premature claims about spec-driven builds or signing.
- `Lovelace`: current pass constrained the package-report increment to a success-only JSON contract with absolute artifact paths and narrow embedded-settings evidence, and explicitly rejected broader release metadata, signing claims, or spec/build ownership that the current command still does not have.
- `Mendel`: current pass forced two honesty checks on the next DX increment: default `app-image` could only count once it had direct E2E proof, and success-only report claims needed a downstream packager-failure case in addition to a preflight validation failure. Both are now covered.
- `Feynman`: current pass constrained the next packaging increment to the smallest honest `railix.app.yaml` subset the runtime can already support: `app.name`, declared `app.profiles`, and `app.settings.defaults` resolved into the existing packaged JSON resource contract, while explicitly rejecting fake dependency-resolution or YAML-conversion claims.
- `Shipwright`: current pass challenged the remaining Slice 5 honesty gap after app/profile metadata ownership and correctly prioritized dependency-aware module selection from `app.dependencies[*].id` over premature YAML conversion or signing theater; that module-selection boundary is now implemented and covered through the public `railix package --app ...` entrypoint.
- `Pathfinder`: latest planning pass confirmed `Slice 6: Standard packs` was the next honest executable milestone before protocol work began, because packaged UI/static-assets mode still has no real source/build/runtime surface in the repo while `StepProvider`-backed pack execution already does.
- `Rootkeeper`: latest planning pass confirmed the no-leak target remains blocked; current proof covers kernel-managed resource ownership and bounded signal replay, but not soak or leak instrumentation for long-lived packaged runtime sessions.
- `Pasteur`: latest planning pass confirmed the remaining packaged UI/static-assets deliverable has no real implementation surface in the repo today, so continuing `Slice 5: Packaging` as if it were a shell-only increment would be roadmap theater.
- `Cicero`: latest planning pass confirmed the no-leak target is still blocked by the deprecated full-history `RunSignalLog.signals()` compatibility path, unmanaged resources outside the kernel registry, and the absence of packaged-runtime soak or leak instrumentation evidence.
- `Tesla`: current store pass confirmed the repo had no executable store contract beyond thin manifests/docs, which forced the smallest honest first slice: filesystem-backed keys, context/payload conventions, and launcher-boundary proof before metadata or backend expansion.
- `Curie`: current store review found two real defects before the milestone moved: explicit `null` writes were rejected despite the `any` contract, and `entry.json` writes were non-atomic. Both are now fixed and covered by regression tests, along with ctx-over-payload precedence, invalid root/key diagnostics, and corrupt-entry failure.
- `Ptolemy`: current metrics exploration confirmed the smallest honest `railix.std.metrics` slice is a thin kernel emission boundary plus one provider-backed `MetricEmit` step with in-process and named-module proof, not another packaging-heavy increment.
- `Hubble`: current metrics review found the real pre-implementation risks before the slice moved: no public step-owned metric emission path, static contract metadata conflicting with generic runtime metric names, and undeclared label/name failure paths that needed direct kernel-boundary tests.
- `Pathfinder`: current audit exploration confirmed the missing step-facing audit boundary, the smallest honest `railix.std.audit` shape (`AuditScope` plus `AuditCustom`), and the need to keep the first provider narrow instead of inventing a fake audit catalog.
- `Gatekeeper Reviewer`: current audit review forced the contract honesty callout that this slice is object-only and unconstrained by `StepContract`, so the docs/spec now say that plainly and the tests pin retry, rejection, and persistence behavior directly at the public boundary.
- `Rootkeeper`: current audit/runtime pass confirmed the safe shape is immediate append with no extra audit buffering, while keeping the no-leak claim blocked by large-payload pressure, deprecated full-history replay, and lack of soak or leak instrumentation.
- `Pathfinder`: current file exploration recommended a narrower `FileRef`-first slice, which forced the implementation write-up to stay explicit that the landed file pack is only a configured-root JSON IO slice, not a generic secure filesystem abstraction.
- `Gatekeeper Reviewer`: current file review challenged fake permission and lifetime claims, which is why the progress doc now calls out that `resource.*` permissions are still spec-only at the kernel boundary and that `railix-std-file` remains a narrow first slice.
- `Rootkeeper`: current file/runtime pass confirmed the safe rule for now is synchronous open/use/close within each file step and durable descriptor refs rather than live handle refs, while keeping no-leak and large-file claims blocked by missing soak, streaming, and symlink-hardening evidence.
- `Descartes`: current file-path pass confirmed the smallest honest next increment was provider-local `FileRef` and `resource.created` path redaction plus dual-mode relative/legacy-absolute ref resolution, not a fake provenance redesign.
- `Rawls`: current runtime pass confirmed relative-path emission does not create a new handle-leak hazard by itself, while calling out the still-open run-lifetime memory tax from no-op durable-file resource registrations.
- `Godel`: current file hardening review forced the explicit remaining-risk list to stay honest: symlink escape had to be proven before the milestone moved, path-based refs are still forgeable as capabilities, and runtime evidence had to assert redacted resource metadata directly.
- `Locke`: current file hardening pass confirmed the next honest move was provider-only symlink rejection across write/read/delete, including relative-path access and absolute-ref leaf symlink escapes, not kernel theatrics.
- `Kierkegaard`: current retention review confirmed the durable-file no-op closer issue is real but not an OS-handle leak, and that any honest fix is kernel-level lifecycle/provenance work rather than a provider-only patch.
- `Kierkegaard` follow-up: constrained the lifecycle fix to a kernel-owned provenance-versus-owned-resource split, which is now the landed shape for durable std-file writes instead of fake provider-local cleanup.
- `Bernoulli`: current provenance review challenged the fake-capability risk directly and pushed the smallest honest next step toward same-run kernel resource lookup for newly issued file refs instead of pretending cross-run persistence already exists.
- `Mill`: current provenance review validated the run-local registry boundary as acceptable only if it stores metadata, not live resources, and is dropped with run lifetime rather than promoted into a fake durable capability store.
- `Pathfinder`: current `std-data` gap review confirmed the honest starting boundary for `DataTransform` was step-local config plus declarative mapping, not fake repeat/operator claims unsupported by the runtime surface that existed at the time.
- `Gatekeeper Reviewer`: current `std-data` review found three real issues in the first pass and all are now fixed before the ledger moved: `DataTransform` now enforces `ctx|reply` writes instead of mutating ingress roots, it now works against the `ContextDoc` SPI instead of only `InMemoryContextDoc`, and plan JSON decoding now defaults missing historical `permissions` blocks instead of breaking backward compatibility.
- `Euler`: current verification pass confirms the repeat/selector slice is clean at the reactor level once run in a solo slot; the earlier `143`/hung `railix-std-data` surefire result was contested workspace state, and the clean rerun finished green end to end.
- `Boole`: current `DataValidate` gap review constrained the first validation slice to required-path presence checks plus valid/invalid branching, which is why the landed step does not pretend to own a richer rule DSL, selector predicates, or reply construction yet.
- `Pathfinder`: current `DataForEach` review found two real correctness risks before the ledger moved: unsafe `statePath` reuse could erase live `ctx` data, and cursor-field alias collisions could corrupt iteration state. Both are now rejected explicitly and covered through provider tests.
- `Schrodinger`: current verification pass confirmed the focused `DataForEach` execution proofs were green before the broader class-suite regression was chased into the macOS builder-emitted packaged-settings path, which is now green again after runtime-image path isolation in the packaging script surface.
- `Erdos`: current `DataAggregate` review constrained the first slice to selector-driven `count|collect|sum|min|max` only, rejected fake `group` breadth, and called out the missing aggregate editor manifest entry before the milestone moved.
- `Pathfinder`: current Creator review confirmed the smallest honest next `Slice 3` move was to replace the placeholder Context Diff / raw signal dump with structured run timeline and changed-path views derived only from persisted `signals.ndjson` plus `summary.json`, not to invent external attach.
- `Rootkeeper`: current Creator/runtime review constrained the new slice to a single-pass bounded artifact reader on active sessions or the newest persisted run only, and pushed malformed or partially written `signals.ndjson` lines into partial artifact-read metadata instead of letting them take down `/api/workspace`.
- `Pathfinder`: current follow-up Creator review argued that the next honest viewer slice was a bounded metric panel over persisted `metric.emitted` signals and that a full audit browser would be dishonest breadth.
- `Rootkeeper`: current follow-up Creator/runtime review forced the metric and audit summaries to project only bounded scalar-or-shape previews plus capped keys/counts, not raw nested payloads, and reiterated that `summary.json` discovery failure is still a real separate risk.
- `Pathfinder`: current historical-drill-in review confirmed the next honest Creator increment was a separate one-run persisted detail boundary plus UI selection from the existing runs list, not `/api/workspace` inflation and not external attach theater.
- `Rootkeeper`: current historical-drill-in/runtime review forced the selector to stay on validated `appId` / `flowId` / `runId` identity rather than client-controlled paths, and it kept raw summary blobs and terminal-failure payloads out of the new historical detail response.
- `Pathfinder`: current summary-hardening review confirmed the next honest Creator stability increment was in-band malformed-`summary.json` degradation for persisted run discovery and detail lookup, not a bigger feature slice dressed as hardening.
- `Rootkeeper`: current summary-hardening/runtime review forced the degraded malformed-summary path to stay at `200` for valid run selectors, preserve signal-derived evidence, and cap `summaryReadError` instead of letting decode diagnostics become their own payload spike.
- `Galileo`: current Creator/runtime review confirmed the last honest `Slice 3` hardening gap lived in launcher-owned output retention rather than another registry-only preview truncation pass, and it flagged total-size versus retained-preview semantics so the landed shape now exposes explicit `stdoutSize` / `stderrSize` plus truncation booleans.
- `Linnaeus`: current test review confirmed there was no honest public-E2E overflow proof through the built-app sample because the shipped launcher path emits only short lines, which is why the closure evidence uses one bounded launcher seam test plus the existing public-shell suite instead of inventing a fake oversized app run.
- `Tesla`: current packaging review confirmed the smallest honest Creator packaging increment was a thin creator-specific launcher preset over the existing modular scripts, and it caught one real risk in the first draft: `app-image` packaging had to forward `APP_MODULES` explicitly or Creator mode could silently fall back to the old headless runtime image.
- `Popper` (`Bytewright`): completed the final bounded Step Contract 1.0 review with no actionable findings across contract freezing, structural assignability, deterministic connection/config/output validation, or mutable-state and retention hazards.
- `Kierkegaard` (`Gatekeeper`): completed the final public-boundary gap review with no missing reachable Step Contract 1.0/connection scenarios and independently confirmed `96.59%` line and `90.76%` branch coverage.

## Next planned work

1. Start `Slice 4: DataWorkbench / Contract-Aware Connection Authoring` by projecting Step Contract ports, shapes, configuration defaults, and semantics into the Creator and rejecting incompatible edges before persistence.
2. Add ordered connection-operator editing plus nested primitive-tree preview, then prove JSON/YAML save/reload equivalence and every supported/rejected authoring path through the real browser or Creator HTTP boundary.
3. Resume `Slice 10: Remote execution boundary` after this authoring milestone, while keeping no-leak, soak, installer, and release certification work explicit under `Slice 11` rather than hidden as completed foundation.
