id: railix.std.data.DataForEach
version: 0.1.0
kind: normal
displayName: Data For Each
summary: Prepare the next selector match in context and emit loop outcomes for the plan graph.
outcomes:
  item: {}
  done: {}
  empty: {}
config:
  selector: { type: selector, required: true }
  as: { type: string, default: item }
  statePath: { type: path, default: ctx.foreach }
  parentAliases: { type: object, required: false }
ui:
  editor: dataworkbench.foreach
