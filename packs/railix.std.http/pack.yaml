id: railix.std.http
version: 0.1.0
name: Railix Standard HTTP Pack
provides:
  sourceSteps:
    - steps/HttpTrigger.step.yaml
  replyAdapters:
    - HttpReplyAdapter
  trafficPanels:
    - panels/http-traffic.panel.yaml
settings:
  - settings.http.host
  - settings.http.port
  - settings.http.tls.enabled
