<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>dev.nanonative.railix</groupId>
    <artifactId>railix-ii-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <modules>
        <module>modules/railix-core</module>
        <module>modules/railix-stdlib</module>
        <module>modules/railix-creator</module>
    </modules>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.outputTimestamp>2000-01-01T00:00:00Z</project.build.outputTimestamp>
        <junit.version>6.0.1</junit.version>
        <assertj.version>3.27.6</assertj.version>
        <maven.clean.version>3.5.0</maven.clean.version>
        <maven.compiler.version>3.15.0</maven.compiler.version>
        <maven.jar.version>3.5.0</maven.jar.version>
        <maven.resources.version>3.4.0</maven.resources.version>
        <maven.surefire.version>3.5.4</maven.surefire.version>
        <jacoco.version>0.8.15</jacoco.version>
        <maven.shade.version>3.6.1</maven.shade.version>
        <playwright.version>1.61.0</playwright.version>
        <jacocoUnitArgLine></jacocoUnitArgLine>
        <jacocoDesktopArgLine></jacocoDesktopArgLine>
        <jacocoMobileArgLine></jacocoMobileArgLine>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven.clean.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven.jar.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven.resources.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.version}</version>
                    <configuration>
                        <argLine>@{jacocoUnitArgLine}</argLine>
                        <failIfNoTests>true</failIfNoTests>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <executions>
                        <execution>
                            <id>coverage-unit-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                            <configuration>
                                <propertyName>jacocoUnitArgLine</propertyName>
                                <destFile>${project.build.directory}/jacoco-unit-${surefire.forkNumber}.exec</destFile>
                                <append>false</append>
                                <includes>
                                    <include>dev.nanonative.railix.*</include>
                                </includes>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
